<resources>
    <!-- Minimal Compose-only theme -->
    <style name="Theme.MiniComposeStarter" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor">@android:color/black</item>
    </style>
</resources>