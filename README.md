# Starter Tasks

1. Wire `NavController` into `HomeScreen` and add an `AboutScreen` destination.
2. Display `CounterViewModel.count` in Home and add an **Increment** button.
3. Add a **Go to About** button to navigate.
4. (Bonus) Pass a string argument to About and display it.
